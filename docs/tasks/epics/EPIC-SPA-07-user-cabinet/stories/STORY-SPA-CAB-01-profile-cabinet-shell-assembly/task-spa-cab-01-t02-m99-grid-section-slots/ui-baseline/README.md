# UI baseline — SPA-CAB-01-T02 (pointer)

**Canonical CAB-01 screenshots (story root):**  
[`../../screenshots/`](../../screenshots/) — `archive/` (UI-0/UI-3 baselines) + `full-cycle/` (happy/edge re-run).

PNG files were moved out of this task folder. Do not write new evidence under `ui-baseline/` — use story `screenshots/`.

**Route:** `/#/profile` · **Viewport:** 1536×1024

## Capture

```bash
cd spa-app && npm run test:ui:cabinet-shell-cab01-full
# legacy archive rewrite (mock): npm run test:ui:cabinet-shell-cab01
```
