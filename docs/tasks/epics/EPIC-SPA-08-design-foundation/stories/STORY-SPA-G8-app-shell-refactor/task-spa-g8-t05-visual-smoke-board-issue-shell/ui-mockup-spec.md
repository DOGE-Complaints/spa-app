# UI mockup spec — SPA-G8 (parity-only)

**ui_anchor:** T05  
**Artboard:** none (no `@mockup:` — same pattern as G4/G7)  
**SSOT:** live Board/Issue shell chrome + [design-system.md](../../../../../../../UX/design-system.md) §4.1/4.2

## Contract

- Header: logo + SYNCED + flag LanguageSelector (`header-locale`)
- Sidebar: Workspace + Board (active) / Issues / Settings
- BoardPage + IssuePage compose via `AppShell` slots; cabinet default NavLinks unchanged
- Visual success = no material layout shift vs `ui-baseline/pre-implement/`

## Human gate

Accepted as parity-smoke (operator P3 Execute); no new artboard.
