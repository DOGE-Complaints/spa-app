/**
 * Product SSOT: gateway governed label set (DOGEIssueLabel in enums.py).
 * Curated translated core labels. This list is NOT the source for board filter options.
 * Board filter options are aggregated from loaded issue labels (L10N-02, D9).
 */
export const AVAILABLE_LABELS = Object.freeze([
  'waste',
  'district',
  'infrastructure',
  'safety',
])
